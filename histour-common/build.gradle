dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}

allprojects {
    tasks.bootJar {
        enabled = false
    }

    tasks.jar {
        enabled = true
    }
}

